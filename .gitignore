/target
/reference/build
/harness/out
*.o
*.mod
/reference/hb_high_ref.f
/reference/hb_high_subs.f
/harness/out
*.gcda
*.gcno
*.gcov

# Reduced-n validation runs (bisection artifacts). The certified baselines are
# the unsuffixed files and are written only with --baseline.
harness/science_*_n*.csv

# Python virtualenv for the extension build
.venv/
*.so
*.egg-info/

# Paper PDFs for the physics annotations. papers/README.md IS committed and records
# citation, source and verification status for each; the PDFs themselves are ~10 MB of
# publisher-copyright material and do not belong in git history.
papers/*.pdf
__pycache__/
