[package]
name = "validate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Scientific equivalence campaign for the hb_high port: IM distributions and inter-frequency correlation"

[dependencies]
im = { path = "../im" }
